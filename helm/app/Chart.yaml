apiVersion: v2
name: template-api-java
description: Helm chart for the template-api-java API
type: application
icon: https://raw.githubusercontent.com/helm/helm/main/docs/logos/helm.svg

# Local placeholder only — release.yml overrides both at publish time:
# helm package --version $VERSION --app-version $VERSION
version: 0.1.0
appVersion: "0.0.0"
