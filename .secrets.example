# ----------------------------------------------------------------------------
# .secrets.example
#
# Example secrets file for local development.
#
# Copy this file to `.secrets` and populate with real values.
# NEVER commit `.secrets` to version control.
# ----------------------------------------------------------------------------

# GitHub App ID
# Used for generating short-lived GitHub App tokens in CI or local tooling.
GH_APP_ID=

# GitHub App Private Key
# The private key for the GitHub App (PEM format).
# For multiline keys, either:
# 1. Use literal newlines, or
# 2. Replace newlines with \n if your tooling requires it.
GH_APP_PRIVATE_KEY=
