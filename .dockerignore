# Git metadata (never needed in image)
.git
.gitignore

# IDE / editor
.vscode
.idea
*.iml
*.ipr
*.iws

# OS junk
.DS_Store

# Local build outputs (container builds its own)
build/
.gradle/

# Logs & temp files
*.log
hs_err_pid*
replay_pid*

# Environment files (never bake secrets)
.env
.env.*

# Docs / misc (optional but keeps context small)
HELP.md
docs/
architecture/

# Exclude all JARs by default…
*.jar

# …but ALWAYS include the Gradle wrapper JAR
!gradle/wrapper/gradle-wrapper.jar

# Node / semantic-release (not used in Docker build)
node_modules/
.npm/
.pnpm-store/
.yarn/
.cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache

# Release tooling/config (CI-only; not needed to build the container)
package.json
package-lock.json
.releaserc.json
